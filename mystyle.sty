% !TEX root = thesis.tex

%
% Separate out the preamble
%

\ProvidesPackage{mystyle}

\usepackage[usenames,dvipsnames,svgnames,table]{xcolor} % allow text colors

\usepackage{float} % force locations with [H]
\usepackage[section]{placeins} % floats stay in section
\usepackage{subcaption} % subfigures

\usepackage[title]{appendix}

\usepackage{fancyhdr} % headers
\fancypagestyle{plain}{%
    \fancyhf{}
    \fancyhead[CE]{\leftmark}
    \fancyhead[CO]{\rightmark}
    \fancyfoot[L]{\docversion}
    \fancyfoot[C]{\hspace{3cm}\thepage}
    \renewcommand{\headrulewidth}{0pt}
}
\fancypagestyle{front}{%
    \fancyhf{}
    \fancyfoot[L]{\docversion}
    \fancyfoot[C]{\hspace{3cm}\thepage}
    \renewcommand{\headrulewidth}{0pt}
}
\fancypagestyle{main}{%
    \fancyhf{}
    \fancyfoot[L]{\docversion}
    \fancyfoot[C]{\hspace{3cm}\thepage}
    \renewcommand{\headrulewidth}{0pt}
}
\pagestyle{fancy}

\usepackage{pdfpages}

\usepackage[layout=a4paper,left=3cm,right=2cm,top=2cm,bottom=2cm]{geometry} % margins

\usepackage{amsfonts} % for mathbb
\usepackage{mathtools} % math
\usepackage{fixmath} % fix greek letters

\usepackage{xfrac} % half etc.

\usepackage{amsthm} % theorems

% URW Century Schoolbook with Fourier Math fonts
%\usepackage{fouriernc}
%\usepackage[T1]{fontenc}

% Garamond
\usepackage[osf]{garamondx}
\usepackage[garamondx,cmbraces]{newtxmath}
\usepackage[T1]{fontenc}

\usepackage{setspace} % allows 1.5 and double spacing
\onehalfspacing
\usepackage{microtype} % resizes letters to fill lines better
\usepackage[english]{babel} % hyphenation

\usepackage{url} % urls
\usepackage[natbibapa]{apacite} % APA citation format
\bibliographystyle{apacite} % use the style
\usepackage[nottoc]{tocbibind} % include bibliography in TOC

\usepackage[intoc,prefix,refpage]{nomencl} % nomenclature (notataio)

\usepackage[pdftex,plainpages=false,pdfpagelabels]{hyperref} % links
\newcommand*{\Appendixautorefname}{Appendix} % add name for appendix refs
%\usepackage[noabbrev,nameinlink]{cleveref}

\renewcommand\nomname{Notation} % change the title from nomenclature to notation
\renewcommand{\pagedeclaration}[1]{, \hyperlink{page.#1}{page\nobreakspace#1}}

\makenomenclature % build the nomenclature


%
% End of packages
%

