% !TEX root = thesis.tex

%
% Separate out the preamble
%

\ProvidesPackage{mystyle}

% widow and orphan control
\widowpenalty10000
\clubpenalty10000

\usepackage[usenames,dvipsnames,svgnames,table]{xcolor} % allow text colors

\usepackage{float} % force locations with [H]
\usepackage[section]{placeins} % floats stay in section
\usepackage[width=0.8\textwidth]{subcaption} % subfigures and subtables
\captionsetup{subrefformat=parens,width=0.9\textwidth} % narrower figure and table captions
\usepackage{wrapfig} % wrap text around figures with wrapfigure

\usepackage[titletoc,title]{appendix}

%% Headers
\usepackage[automark]{scrlayer-scrpage}
\ofoot{\docversion}
\cfoot{\pagemark}
\ohead{} % clear outer header
\ihead{} % clear inner header
\chead{} % clear middle header

\usepackage{pdfpages}

\usepackage[layout=a4paper,left=3cm,right=2cm,top=2cm,bottom=2cm,headheight=20pt]{geometry} % margins

\usepackage[pdftex,plainpages=false,pdfpagelabels]{hyperref} % links
\newcommand*{\Appendixautorefname}{Appendix} % add name for appendix refs

\usepackage{amsfonts} % for mathbb
\usepackage{mathtools} % math
\usepackage{fixmath} % fix greek letters

\usepackage[noabbrev,nameinlink]{cleveref} % \cref command (this must come after amsmath)

\usepackage{xfrac} % half etc.

\usepackage{amsthm} % theorems

% URW Century Schoolbook with Fourier Math fonts
%\usepackage{fouriernc}
%\usepackage[T1]{fontenc}

% Garamond
\usepackage[osf]{garamondx}
\usepackage[garamondx,cmbraces]{newtxmath}
\usepackage[T1]{fontenc}

\usepackage{setspace} % allows 1.5 and double spacing
\onehalfspacing
\usepackage{microtype} % resizes letters to fill lines better
\usepackage[english]{babel} % hyphenation

\usepackage{url} % urls
\usepackage[natbibapa]{apacite} % APA citation format
\bibliographystyle{apacite} % use the style in the bibliography
\usepackage[nottoc]{tocbibind} % include bibliography in TOC

\usepackage[toc,acronym]{glossaries} % use glossaries package for Notation
\setacronymstyle{long-short} % show full phrase the first time then just acronym
\newglossary[nlg]{notation}{not}{ntn}{Notation} % define a glossary for notation

\usepackage{booktabs} % fancy tables

\usepackage{listings} % code block or inline

\loadglsentries{glossary} % all notation in here
\loadglsentries{notation} % all notation in here
\makeglossaries % build the Glossary and Notation
\loadglsentries{acronyms} % all acronyms in here
\makeglossaries % build the Acronym (need twice for @see)

%
% contents of latexmkrc
%
% add_cus_dep('glo', 'gls', 0, 'makeglossaries');
% add_cus_dep('acn', 'acr', 0, 'makeglossaries');
% sub makeglossaries{
%   system( "makeglossaries \"$_[0]\"" );
% }

%
% End of packages
%

